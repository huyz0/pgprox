# Surviving mutants that are accepted, with the reason each is accepted.
#
# `scripts/mutants.sh` fails on any survivor that is not in this file. That is
# the whole mechanism: the list may not grow without somebody writing down why.
#
# One entry per line, the key and the reason separated by a tab. The key is the
# crate, the file, the function and the replacement text, with no line number,
# so editing the lines above a mutant does not invalidate its entry.
#
# # Where this list came from
#
# `M10.3` ran the tool for the first time, three milestones after
# `standards/testing.md` started claiming that it ran nightly. It found 89
# survivors out of 950 mutants across the four sans-I/O crates: 720 caught, 137
# unviable, 89 alive. That is an 89% kill rate against line coverage of 96% to
# 99%, which is the gap the standard's own sentence about coverage being a floor
# is about.
#
# Every entry started as `untriaged`, which is not a reason. It is a statement
# that the tool ran before anybody had read its output, which is the honest
# order: the gate protects against new survivors from today, and `M10.4` through
# `M10.8` are the tasks that turn each of these into either a test or a real
# reason. `untriaged` may not be used for anything added after those.
#
# `M10.4` did `pgprox-cache`: ten survivors became one. Six fell to four tests
# about the byte accounting, three more to two tests about the recency index and
# the expiry guard, and the last is a timeout with a reason below. That is what
# this file is for: the count only goes down, and every line that stays says why.
#
# `M10.7` and `M10.10` did `pgprox-proto`. Between them nine survivors fell to
# four tests, and the six that remain are equivalent mutants: programs that
# differ from the original in their text and not in anything they can be made to
# do. That is the answer nobody should reach for first, so each one below
# carries the argument rather than the assertion: two operands with no bit in
# common, a `usize` compared against zero, a guard in front of a callee that
# already guards, an empty match arm that exists to hold a comment, a `continue`
# whose case the code below it drops anyway. An equivalent mutant is a claim
# about the code, and the way to tell one from a missing test is to write down
# why no test could distinguish the two programs, then let a reader disagree.

# --- pgprox-cache ---
pgprox-cache|crates/pgprox-cache/src/store.rs|Inner::remove|None	the body becomes `None`, so nothing is removed and `evict_to_fit` never terminates. The suite does detect it, by hanging, which the tool reports as a timeout rather than a kill. There is no assertion that improves on a hang, and `evict_to_fit` already returns rather than loops when the index and the map disagree, which is the real hazard this shape points at.

# --- pgprox-proto ---
pgprox-proto|crates/pgprox-proto/src/read.rs|Reader<'a>::cstr|-	caught, and reported as a timeout. `pos -= end + 1` underflows and the proptest `reading_never_panics_on_arbitrary_input` fails, then spends its budget shrinking. Whether that lands inside the per-mutant timeout depends on machine load, which is why this one appears and disappears between runs: the timeout category here means slow, not undetected
pgprox-proto|crates/pgprox-proto/src/backend.rs|conn_id_from_key|^	equivalent. `(u64::from(pid as u32) << 32) | u64::from(secret as u32)`: the left operand's low thirty-two bits are all zero and the right operand's high thirty-two are, so the two operands share no set bit and OR, XOR and ADD agree on every input. No test can tell the three apart
pgprox-proto|crates/pgprox-proto/src/relay.rs|FrameRelay::push_body|>=	equivalent. `self.want_inspect > 0` becomes `>= 0` on a `usize`, which is always true, and the body it then runs is `copy = 0.min(take)`, an `extend_from_slice` of an empty slice and a subtraction of zero. The guard is an optimisation, not a decision, and the mutant only removes the optimisation
pgprox-proto|crates/pgprox-proto/src/relay.rs|FrameRelay::push_header|*	equivalent. `1 + LEN_PREFIX` becomes `1 * LEN_PREFIX`, four rather than five, so four buffered bytes stop taking the early return. They reach `decode_header` instead, whose own first line returns `Ok(None)` below five bytes, and the `else` branch returns the same `RelayOutcome` the early return would have. `need` on the line above is unmutated, so the buffer still never exceeds five. `M10.10` filed this expecting a length field read from a byte that had not arrived; the callee's guard is what makes that impossible
pgprox-proto|crates/pgprox-proto/src/relay.rs|FrameRelay::push_header|>	equivalent, and for the same reason as the entry above. `len < 5` becomes `len > 5`, which never holds because `need` caps the buffer at five, so every partial header falls through to `decode_header` and comes back `Ok(None)`. The early return duplicates a check the callee already makes; that is what both of these mutants are reporting
pgprox-proto|crates/pgprox-proto/src/session.rs|SessionState::on_frontend|	equivalent. The deleted arm is `FrontendMessage::Sync => {}`, whose body is empty. Without it `Sync` falls to the guarded arm, and `starts_extended_sequence` lists Parse, Bind, Execute, Describe and Close but not Sync, so it falls again to `_ => {}`. The arm exists to hold the comment above it, which is the reason a reader needs and the compiler does not
pgprox-proto|crates/pgprox-proto/src/startup.rs|Startup<'_>::options|&&	equivalent. `setting.is_empty() || setting == "-c"` becomes `&&`, which no string satisfies, so nothing is skipped. Both skipped values then reach `setting.split_once('=')`, which returns `None` for `""` and for `"-c"` because neither contains an equals sign, and nothing is pushed either way. The `continue` names the case instead of leaving it to the absence of a separator

# --- pgprox-route ---
pgprox-route|crates/pgprox-route/src/classify.rs|classify_one|(Default::default(), true)	as above, a whole-body replacement that does not terminate
pgprox-route|crates/pgprox-route/src/classify.rs|classify|	the body is replaced wholesale and the mutant does not terminate; the suite detects it by hanging, which the tool reports as a survivor rather than a kill
pgprox-route|crates/pgprox-route/src/hints.rs|split_block_comment|*=	as above: `i *= 1` is a no-op and the scan never advances
pgprox-route|crates/pgprox-route/src/hints.rs|split_block_comment|-=	the scan index stops advancing, so the walk over a block comment never ends; a hang rather than a wrong answer
pgprox-route|crates/pgprox-route/src/hints.rs|split_block_comment|<=	equivalent. The loop guard becomes `i <= bytes.len()`, and at `i == len` the slice `bytes[i..]` is empty rather than out of bounds, so the body matches nothing, adds one, and the loop exits with the same result one iteration later
pgprox-route|crates/pgprox-route/src/hints.rs|statement_hint|==	as above

# --- pgprox-session ---
pgprox-session|crates/pgprox-session/src/cancel.rs|send|Ok(())	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/probe.rs|run_replica_query|!=	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/probe.rs|text_row|<=	untriaged
pgprox-session|crates/pgprox-session/src/sequence.rs|HeldSequence::may_hold|||	equivalent. The `Parse` arm reads `self.frames.is_empty() && self.step == Step::Nothing`, and those two are the same boolean at every reachable state: `feed` only reaches `may_hold` with an empty buffer for a `Parse` or a `Bind`, `record` and the push into `frames` happen together, and `clear` resets both. `A && A` and `A || A` agree, so the second conjunct is a restatement of the first rather than a second condition
pgprox-session|crates/pgprox-session/src/sequence.rs|begins|true	equivalent. `begins` guards the first frame of a sequence, and making it accept everything only lets a message reach `may_hold`, which with an empty buffer returns true for a `Parse` or a `Bind` and false for every other arm: `Describe` and `Execute` both require `Step::Bound` and the catch-all is false. So the frames the guard would have turned away are turned away one check later, with the same `Held::Send`. The guard is worth keeping for saying so at the top of the function, and no test can distinguish the two programs
pgprox-session|crates/pgprox-session/src/shell.rs|?|+	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::flush|Ok(())	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::flush|	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::flush|||	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::queue|()	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|accept|Ok(())	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|authenticate_scram|Ok(())	a write path replaced by a no-op, detected by the suite failing to terminate rather than by an assertion. Reproduced by hand for `flush -> Ok(())`: five probe tests wait for bytes that are never written and run past sixty seconds. The other five are the same shape. These five were not in `M10.3`'s baseline and appear now, which is the load dependence already recorded for `Reader::cstr`: whether a hang lands inside the per-mutant timeout depends on the machine. `M10.13` owns the question of whether hang-detection is good enough for this file
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::borrow|-	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::borrow|<	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::buffered|Vec::leak(Vec::new())	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::buffered|Vec::leak(vec![0])	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::buffered|Vec::leak(vec![1])	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::compact|!=	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::compact|()	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::consume|()	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::consume|*=	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::fill_held|*	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::fill_held|*	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::fill_held|Ok(())	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::fill|Ok(())	untriaged (timeout: the mutant made something not terminate)
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::is_buffered|>=	untriaged
pgprox-session|crates/pgprox-session/src/shell.rs|Wire<S>::reclaim|>	untriaged (timeout: the mutant made something not terminate)
