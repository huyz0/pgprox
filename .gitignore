/target
/target-coverage
# cargo-fuzz builds into its own directory, since fuzz/ is a separate workspace.
/fuzz/target
/fuzz/artifacts
/fuzz/coverage
**/*.rs.bk
*.profraw
*.profdata
/coverage
perf.data*
flamegraph.svg
callgrind.out.*
dhat-heap.json
.env
*.pem
*.key

# Upstream proxies cloned for protocol reference. Read-only, never vendored.
/reference
